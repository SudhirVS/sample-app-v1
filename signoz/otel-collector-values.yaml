otelCollector:
  config:
    receivers:
      filelog:
        include:
          - /var/log/pods/*/*/*.log
        start_at: beginning
        include_file_path: true
        operators:
          # Step 1: Parse Docker JSON log wrapper {"log":"...","stream":"stdout","time":"..."}
          - type: json_parser
            id: parse_docker_json
            timestamp:
              parse_from: attributes.time
              layout: '%Y-%m-%dT%H:%M:%S.%LZ'
            on_error: send
          # Step 2: Move raw log string to body
          - type: move
            from: attributes.log
            to: body
          # Step 3: Parse app-level JSON from body
          - type: json_parser
            id: parse_app_json
            on_error: send
          # Step 4: Extract namespace and container from file path
          - type: regex_parser
            id: extract_metadata
            regex: '^/var/log/pods/(?P<k8s_namespace>[^_]+)_(?P<pod>[^_]+)_[^/]+/(?P<container>[^/]+)/'
            parse_from: attributes["log.file.path"]
            on_error: send

    processors:
      batch:
        send_batch_size: 10000
        send_batch_max_size: 11000
        timeout: 10s
      # Tag every log record with namespace, service name and cluster
      resource/labels:
        attributes:
          - key: k8s.namespace.name
            from_attribute: k8s_namespace
            action: insert
          - key: service.name
            from_attribute: container
            action: insert
          - key: k8s.cluster.name
            value: "minikube"
            action: insert

    service:
      pipelines:
        logs:
          receivers: [otlp, filelog]
          processors: [batch, resource/labels]
          exporters: [clickhouselogsexporter]

  # Mount host log paths — SigNoz collector runs inside Minikube so /var/log/pods is native
  extraVolumes:
    - name: varlogpods
      hostPath:
        path: /var/log/pods
    - name: varlibdockercontainers
      hostPath:
        path: /var/lib/docker/containers

  extraVolumeMounts:
    - name: varlogpods
      mountPath: /var/log/pods
      readOnly: true
    - name: varlibdockercontainers
      mountPath: /var/lib/docker/containers
      readOnly: true
